[project]
name = "glm-local-32gb"
version = "0.1.0"
description = "GLM checkpoint feasibility audit and Windows resource-control foundation"
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
line-length = 100
