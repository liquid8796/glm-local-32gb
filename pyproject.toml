[project]
name = "glm-local-32gb"
version = "0.11.0"
description = "Experimental GLM decoder with bounded FP8 and NVFP4 weight streaming"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
validation = ["numpy>=2.0,<3.0"]
storage-validation = ["numpy>=2.0,<3.0", "safetensors>=0.7,<1.0"]

[tool.ruff]
line-length = 100
