[project]
name = "glm-local-32gb"
version = "0.3.0"
description = "GLM feasibility audit and fixed synthetic CPU/CUDA decoder validation"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
validation = ["numpy>=2.0,<3.0"]

[tool.ruff]
line-length = 100
