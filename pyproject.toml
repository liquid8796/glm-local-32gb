[project]
name = "glm-local-32gb"
version = "0.2.0"
description = "GLM feasibility audit and bounded synthetic FP8 CPU/CUDA probes"
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
line-length = 100
