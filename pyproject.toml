[project]
name = "glm-local-32gb"
version = "0.4.0"
description = "Fixed synthetic CPU/CUDA decoder with pinned official Transformers comparison"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
validation = ["numpy>=2.0,<3.0"]

[tool.ruff]
line-length = 100
