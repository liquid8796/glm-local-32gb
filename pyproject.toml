[project]
name = "glm-local-32gb"
version = "0.5.0"
description = "Synthetic decoder parity and bounded safetensors FP8 block validation"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
validation = ["numpy>=2.0,<3.0"]

[tool.ruff]
line-length = 100
